spring:
  application:
    name: loan-kotlin-api

  profiles:
    active: local

  h2:
    console:
      enabled: true         # H2 Console 활성화
      path: /h2-console     # 접속 경로
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test   # 인메모리 DB
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

  lifecycle:
    timeout-per-shutdown-phase: 30s

  # Kafka 자동설정 비활성화 (로컬 테스트 시 Kafka 없이 기동)
  #autoconfigure:
  #  exclude:
  #    - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: notification-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"

server:
  port: 8080
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
  endpoint:
    prometheus:
      enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      # 모든 매트릭에 애플리케이션 이름 태그 추가
      # Grafana에서 application="notification-service" 로 필터링 가능
      application: notification-service

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
    display-request-duration: true
    groups-order: DESC
    doc-expansion: none
  api-docs:
    path: /api-docs
    enabled: true
  show-actuator: false
  default-consumes-media-type: application/json
  default-produces-media-type: application/json

resilience4j:
  circuitbreaker:
    configs:
      default:

        # Sliding Window 설정
        sliding-window-type: COUNT_BASED
        sliding-window-size: 10           # 최근 10번 호출 기준
        minimum-number-of-calls: 5        # 최소 5번 호출 후 통계

        # CLOSE -> OPEN 전환 조건
        failure-rate-threshold: 50        # 실패율 50% 이상이면 OPEN
        slow-call-duration-threshold: 7s  # 7초 이상이면 느린 호출
        slow-call-rate-threshold: 50      # 느린 호출 50% 이상이면 OPEN

        # OPEN 상태 유지 시간
        wait-duration-in-open-state: 60s  # OPEN 상태 60초 유지

        # HALF_OPEN 상태 설정
        permitted-number-of-calls-in-half-open-state: 3   # 3개 요청 테스트
        automatic-transition-from-open-to-half-open-enabled: true

        # HALF_OPEN -> CLOSE 또는 OPEN 전환
        max-wait-duration-in-half-open-state: 10s         # HALF_OPEN 최대 10초

        # 기록할 예외
        record-exceptions:
          - org.springframework.web.client.HttpServerErrorException
          - org.springframework.web.client.ResourceAccessException
          - java.net.ConnectException
          - java.net.SocketTimeoutException

        # 무시할 예외 (Circuit Breaker에 영향 안 줌)
        ignore-exceptions:
          - org.springframework.web.client.HttpClientErrorException

    instances:
      KAKAO_BANK:
        base-config: default
      TOSS_BANK:
        base-config: default
      LINE_BANK:
        base-config: default


  # Retry 설정
  retry:
    instances:
      default:
        max-attempts: 3
        wait-duration: 1s
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2
        max-wait-duration: 10s
        # ✅ 여기에 추가!
        retry-exceptions:
          - org.springframework.web.client.HttpServerErrorException
          - org.springframework.web.client.ResourceAccessException
          - java.net.ConnectException
          - java.net.SocketTimeoutException
          - java.io.IOException

# Notification Sender Server Configuration
notification:
  sender:
    base-url: http://localhost:8090/send/sms

loan-api:
  partners:
    KAKAO_BANK:
      base-url: http://localhost:8090
      path: /send/sms
      connect-timeout-ms: 3000
      read-timeout-ms: 7000
    TOSS_BANK:
      base-url: http://localhost:8090
      path: /send/sms
      connect-timeout-ms: 3000
      read-timeout-ms: 7000
    KB_CAPITAL:
      base-url: http://localhost:8090
      path: /send/sms
      connect-timeout-ms: 3000
      read-timeout-ms: 7000
    LINE_BANK:
      base-url: http://localhost:8090
      path: /send/sms
      connect-timeout-ms: 3000
      read-timeout-ms: 7000
    SHINHAN_BANK:
      base-url: 127.0.0.1
      port: 9001
      path: /loan/inquiry
      connect-timeout-ms: 3000
      read-timeout-ms: 10000

nice-api:
  dnr:
    base-url: http://localhost:8090
    path: /send/sms
    connect-timeout-ms: 3000
    read-timeout-ms: 7000